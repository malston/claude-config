# ABOUTME: Example environment variables for setup.sh and Docker
# ABOUTME: Copy to .env and fill in your values (gitignored)

# Git identity for commits
GIT_USER_NAME=Your Name
GIT_USER_EMAIL=you@example.com

# GitHub personal access token for cloning private repos and pushing code
# Generate at: https://github.com/settings/tokens
GITHUB_TOKEN=ghp_your_token_here

# Context7 API key for documentation MCP server (optional)
# Get your key at: https://context7.com
CONTEXT7_API_KEY=your_context7_key_here

# Claude configuration repo to deploy extensions (.library/, CLAUDE.md, enabled.json)
# into sandbox containers (optional, requires git access to the repo)
# CLAUDE_CONFIG_REPO=https://github.com/username/claude-config.git
# CLAUDE_CONFIG_BRANCH=main

# Dotfiles repo to clone into container on first run (Docker only, optional)
# DOTFILES_REPO=https://github.com/username/dotfiles.git
# DOTFILES_BRANCH=linux

# Workspace directory to mount (Docker only, optional)
# WORKSPACE=~/workspace
